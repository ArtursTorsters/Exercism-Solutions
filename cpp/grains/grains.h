#if !defined(GRAINS_H)
#define GRAINS_H

namespace grains {

    enum class square { };

    // Function to calculate grains on a chessboard
    unsigned long long square(unsigned int num);
    unsigned long long total();

}  // namespace grains

#endif // GRAINS_H
